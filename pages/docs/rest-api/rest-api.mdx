# Fetching Products Using REST API

## Using `fetch`

#### Construct the URL

```js copy
const storeId = "your-store-id"; //This will be in your settings page when you logged in to the dashboard

const baseUrl = "abacus-psi.vercel.app/api";

const fetchUrl = `${baseUrl}/${store_id}/products`;
```

### Fetch All Products

```js copy
async function fetchProducts() {
	try {
		const response = await fetch(fetchUrl);
		if (!response.ok) {
			throw new Error("Network response was not ok");
		}

		const data = await response.json();
		console.log(data);
	} catch (error) {
		console.error("Fetch error:", error);
	}
}

fetchProducts();
```

### Fetch a Single Product

```js copy
const productId = "your-product-id";
const fetchUrl = `${baseUrl}/${storeId}/products/${productId}`;

async function fetchProduct() {
	try {
		const response = await fetch(fetchUrl);
		if (!response.ok) {
			throw new Error("Network response was not ok");
		}

		const data = await response.json();
		console.log(data);
	} catch (error) {
		console.error("Fetch error:", error);
	}
}

fetchProduct();
```

### Fetch All Categories

```js copy
const fetchUrl = `${baseUrl}/${storeId}/categories`;

async function fetchCategories() {
	try {
		const response = await fetch(fetchUrl);
		if (!response.ok) {
			throw new Error("Network response was not ok");
		}

		const data = await response.json();
		console.log(data);
	} catch (error) {
		console.error("Fetch error:", error);
	}
}

fetchCategories();
```

### Fetch Single Categories

```js copy
const categoryId = "your-category-id";
const fetchUrl = `${baseUrl}/${storeId}/categories/${categoryId}`;

async function fetchCategory() {
	try {
		const response = await fetch(fetchUrl);
		if (!response.ok) {
			throw new Error("Network response was not ok");
		}

		const data = await response.json();
		console.log(data);
	} catch (error) {
		console.error("Fetch error:", error);
	}
}

fetchCategory();
```

### Fetch All Collections

```js copy
const fetchUrl = `${baseUrl}/${storeId}/collections`;

async function fetchCollections() {
	try {
		const response = await fetch(fetchUrl);
		if (!response.ok) {
			throw new Error("Network response was not ok");
		}

		const data = await response.json();
		console.log(data);
	} catch (error) {
		console.error("Fetch error:", error);
	}
}

fetchCollections();
```

### Fetch Single Collections

```js copy
const collectionId = "your-collection-id";
const fetchUrl = `${baseUrl}/${storeId}/collections/${collectionId}`;

async function fetchCollection() {
	try {
		const response = await fetch(fetchUrl);
		if (!response.ok) {
			throw new Error("Network response was not ok");
		}

		const data = await response.json();
		console.log(data);
	} catch (error) {
		console.error("Fetch error:", error);
	}
}

fetchCollection();
```
